Use `Aside` for the coaching sentence — the thing a person would say, not the app.

```jsx
<Aside>No load, no score — go to the first honest resistance and breathe there.</Aside>
```

Never two on a screen. Never for instructions the user must follow to proceed.
