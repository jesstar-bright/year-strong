Use `StatCard` for one number per card. Never two.

```jsx
<StatCard label="Streak" value="17" sub="days in a row" />
<StatCard label="Pull-up" value="2 / 5" sub="strict reps" done />
```

`done` turns the number sage — reserve it for a goal already met, not progress toward one.
