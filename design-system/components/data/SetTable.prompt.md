Use `SetTable` for every logged exercise. The active row takes the pink left rule; done rows go sage and fade.

```jsx
<SetTable sets={[{reps:'1',done:true},{},{},{}]} current={1} onSelect={setCurrent} />
```

For holds swap the header: `headers={['SET','SECONDS']}`.
