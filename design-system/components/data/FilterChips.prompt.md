Use `FilterChips` to narrow a list — recipe type, workout phase, note tag.

```jsx
<FilterChips items={['All','10 min','High iron','Rescue']} value={f} onChange={setF} />
```

Always include an "All" chip first, always selected by default.
