Use `MealRow` for each slot on the Food tab. Three per day, max four options each.

```jsx
<MealRow time="07:30" tag="40 G · IRON" name="Turkey hash" alts="salmon hash · warm oats"
  options={[{key:'A',text:'Turkey, spinach & red pepper hash'}]} />
```
